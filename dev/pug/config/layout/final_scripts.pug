// --- init final_scripts.pug ---
//#region Final Scripts

script(src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js")

script. 
  var options= {};
  document.addEventListener('DOMContentLoaded', function() {
  var elems = document.querySelectorAll('.sidenav');
  var instances = M.Sidenav.init(elems, options);
  });

// --- Floating-Button
script.
    // Objeto con las opciones con las que el objeto será instanciado
      // direction: 'top' | 'right' | 'bottom' | 'left'
      // hoverEnabled: 'true' | 'false'  true se acciona con el hover
      // toolbarEnabled: 'false' | 'true'  con true se convierte en barra
    options= {
      direction: 'left',
      hoverEnabled: false, 
      //- toolbarEnabled: true,
    };
    document.addEventListener('DOMContentLoaded', function() {
    //- Elementos del DOM con los que el plugin fue inicializado
    var elems = document.querySelectorAll('.fixed-action-btn');
    var instances = M.FloatingActionButton.init(elems, options);
  });
// #endregion
// --- end final_scripts.pug ---